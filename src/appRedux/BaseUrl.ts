export const baseUrl = 'https://jpsageerp.herokuapp.com';

export const config = {
	header: {
		'Content-Type': 'application/json',
	},
};

export const fileConfig = {
	headers: { 'Content-type': 'multipart/form-data' },
};
